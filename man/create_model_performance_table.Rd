% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{create_model_performance_table}
\alias{create_model_performance_table}
\title{Create a combined performance table across metrics}
\usage{
create_model_performance_table(
  correctness,
  parsing,
  consistency,
  caption = NULL
)
}
\arguments{
\item{correctness}{Data frame with model accuracy posteriors containing
columns: model_id, .prob, .lower, .upper}

\item{parsing}{Data frame with parsing success posteriors with same structure}

\item{consistency}{Data frame with consistency posteriors with same structure}

\item{caption}{Optional character string for table caption}
}
\value{
A flextable object with combined performance metrics and color coding
}
\description{
Builds a flextable with median posterior accuracy, parsing success, and
consistency side by side for each model. Includes color-coded background
gradients to highlight performance differences.
}
\details{
The function creates a comprehensive performance table by: 1.
Joining the three metric datasets by model_id 2. Formatting values as
percentages with confidence intervals 3. Applying divergent color gradients
based on median values 4. Sorting models by accuracy (descending)

Color coding uses a light divergent palette:
\itemize{
\item Lower values: light orange to white
\item Higher values: white to light blue
\item Gradient switches at the median value for each metric
}
}
\examples{
\dontrun{
create_model_performance_table(
  correctness = accuracy_posteriors,
  parsing = parsing_posteriors,
  consistency = consistency_posteriors,
  caption = "Model Performance Comparison"
)
}

}
